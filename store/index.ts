export const state = () => ({
  counter: 0,
  s3: null,
});

export const mutations = {
  // increment(state) {
  //   state.counter++;
  // },
};

export const actions = {
  // nuxtServerInit({commit}, {$config}) {
    // commit('setS3', s3);
  // },
};

export const getters = {};
